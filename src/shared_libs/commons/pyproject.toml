[project]
name = "commons"
version = "0.1.0"
description = "Contains shared configuration data and utilities."
readme = "README.md"
authors = []
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.28,<0.10.0"]
build-backend = "uv_build"
