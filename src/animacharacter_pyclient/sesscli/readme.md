This subpackage cotains the http api to perform a session request to the http server on the robot.
This also handles all communication with the robot for an ongoing session, using nng and udp sockets. This package is private,
is not ment to be seen by the user, and is used internally by the global api class.