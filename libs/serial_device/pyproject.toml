[project]
name = "serial-transfer-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyserial>=3.5",
    { path = "../package_a", editable = true }
    "tabulate>=0.9.0",
    "typer[all]>=0.20.1",
]
