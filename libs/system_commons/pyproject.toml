[project]
name = "system-commons"
version = "0.1.0"
description = "Shared utilities and commons across the overall system"
readme = "README.md"
authors = [
    { name = "Americo", email = "americo.cherubini2003@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
system-commons = "system_commons:main"

[build-system]
requires = ["uv_build>=0.11.21,<0.12.0"]
build-backend = "uv_build"
