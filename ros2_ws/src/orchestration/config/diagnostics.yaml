diagnostic_aggregator:
  ros__parameters:
    analyzers:
      hardware:
        type: diagnostic_aggregator/GenericAnalyzer
        path: Hardware
        contains: ["hardware_manager"]

      loops:
        type: diagnostic_aggregator/GenericAnalyzer
        path: Hardware/Loops
        contains: ["loop"]