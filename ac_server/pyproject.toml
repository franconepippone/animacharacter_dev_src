[project]
name = "ac-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyserialdevice",
]

[tool.uv.sources]
pyserialdevice = { path = "../libs/pySerialDevice", editable = true }
